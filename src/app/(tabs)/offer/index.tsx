import ScreenWrapper from "@/components/ScreenWrapper";
import Typography from "@/components/Typography";
import React from "react";

const Offer = () => {
  return (
    <ScreenWrapper>
      <Typography.Text>Offer tab</Typography.Text>
    </ScreenWrapper>
  );
};

export default Offer;
