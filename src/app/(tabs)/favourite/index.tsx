import ScreenWrapper from "@/components/ScreenWrapper";
import Typography from "@/components/Typography";
import React from "react";

const Favourite = () => {
  return (
    <ScreenWrapper>
      <Typography.Text>Favourite tab</Typography.Text>
    </ScreenWrapper>
  );
};

export default Favourite;
