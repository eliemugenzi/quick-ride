import ScreenWrapper from "@/components/ScreenWrapper";
import Typography from "@/components/Typography";
import React from "react";

const Profile = () => {
  return (
    <ScreenWrapper>
      <Typography.Text>Profile tab</Typography.Text>
    </ScreenWrapper>
  );
};

export default Profile;
